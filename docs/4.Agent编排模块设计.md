# GraphAgent 三层编排架构设计文档

## 1. 概述

### 1.1 设计灵感

借鉴公司经营管理的三层架构：

| 角色 | 企业类比 | 对应 Agent | 核心职责 |
|------|---------|-----------|---------|
| 高层管理 | CEO/VP | **GuardAgent** | 识别战略意图、审核方案质量、验收最终结果 |
| 中层管理 | 部门经理 | **PlanAgent** | 制定战术方案、调集资源、分配任务、汇总成果 |
| 基层员工 | 技能员工 | **SubAgent** | 执行具体任务、调用工具、反馈结果 |

### 1.2 核心流程

```
用户输入
  → GuardAgent [意图识别]
    → PlanAgent [任务分解 + 方案制定]
      → GuardAgent [方案审核]
        ├─ 驳回 → PlanAgent [修订方案] → GuardAgent [再审]
        └─ 通过 → PlanAgent [派发子任务]
                    ├→ SubAgent_A [执行任务A] → 结果A
                    ├→ SubAgent_B [执行任务B] → 结果B
                    └→ SubAgent_C [执行任务C] → 结果C
                  → PlanAgent [汇总结果 + 生成终稿]
    → GuardAgent [结果审核]
      ├─ 驳回 → PlanAgent [修订] → GuardAgent [再审]
      └─ 通过 → 返回用户
```

### 1.3 设计原则

1. **关注点分离**：每层 Agent 只关注自身职责，不越界
2. **审核闭环**：关键决策点（方案、结果）必须通过 GuardAgent 审核
3. **并行执行**：无依赖的子任务由各自 SubAgent 并行处理
4. **统一消息**：所有 Agent 间通信均使用 `MessageBlock` 格式
5. **渐进增强**：基于现有 `AgentState` 扩展，不破坏已有架构

---

## 2. 模块目录结构

```
src/graph_agent/
├── orchestration/                  # 新增：Agent 编排模块
│   ├── __init__.py                 # 公共接口导出
│   ├── state.py                    # OrchestrationState — 编排专用 AgentState
│   ├── graph.py                    # 编排图定义（主入口）
│   ├── guard.py                    # GuardAgent 节点 + 系统提示词
│   ├── plan.py                     # PlanAgent 节点 + 系统提示词
│   ├── subagent.py                 # SubAgent 注册中心 + 执行节点
│   └── prompts/                    # 各 Agent 的系统提示词模板
│       ├── __init__.py
│       ├── guard_prompt.py         # GuardAgent 提示词
│       ├── plan_prompt.py          # PlanAgent 提示词
│       └── sub_prompt.py           # SubAgent 提示词（可带技能描述）
├── message/                        # 已有：消息模块（复用）
│   └── ...
├── tools/                          # 已有：工具模块（SubAgent 使用）
│   └── ...
├── llm/                            # 已有：LLM 模块（各 Agent 可独立配置）
│   └── ...
└── node/                           # 已有：可在此添加编排节点辅助函数
    └── ...
```

---

## 3. 状态设计

### 3.1 OrchestrationState

编排流程需要比基础 `AgentState` 更丰富的状态信息。在 `AgentState` 基础上扩展：

```python
# src/graph_agent/orchestration/state.py

from typing import Annotated, TypedDict
from dataclasses import dataclass, field
from enum import Enum
import operator

from graph_agent.state import AgentState
from graph_agent.message import MessageBlock


class OrchestrationPhase(Enum):
    """编排流程所处阶段"""
    INTENT_ANALYSIS = "intent_analysis"       # GuardAgent 分析用户意图
    PLAN_GENERATION = "plan_generation"       # PlanAgent 制定方案
    PLAN_REVIEW = "plan_review"               # GuardAgent 审核方案
    TASK_EXECUTION = "task_execution"         # SubAgent 并行执行子任务
    RESULT_SYNTHESIS = "result_synthesis"     # PlanAgent 汇总结果
    RESULT_REVIEW = "result_review"           # GuardAgent 审核最终结果
    COMPLETED = "completed"                   # 任务完成


@dataclass
class TaskPlan:
    """PlanAgent 生成的任务计划"""
    plan_id: str                                    # 计划唯一ID
    overall_goal: str                               # 总体目标描述
    sub_tasks: list[SubTask]                         # 子任务列表
    execution_strategy: str                          # 执行策略说明（串行/并行）
    expected_output_format: str                      # 期望输出格式
    created_at: str = ""                             # 创建时间戳


@dataclass
class SubTask:
    """单个子任务定义"""
    task_id: str                                     # 子任务ID
    description: str                                 # 任务描述
    required_skill: str                              # 所需技能名称
    assigned_agent: str = ""                         # 被分配的 SubAgent 名称
    input_data: dict = field(default_factory=dict)   # 输入数据
    dependencies: list[str] = field(default_factory=list)  # 依赖的其他子任务ID
    status: str = "pending"                          # pending / running / completed / failed
    result: str = ""                                 # 执行结果
    error: str = ""                                  # 错误信息


class OrchestrationState(AgentState):
    """编排状态，继承 AgentState 的双通道消息机制"""
    # 编排流程控制
    phase: OrchestrationPhase                                 # 当前阶段
    intent: str                                               # GuardAgent 识别的用户意图
    guard_feedback: str                                       # GuardAgent 的审核反馈

    # 任务计划
    task_plan: TaskPlan | None                                # 当前任务计划

    # SubAgent 执行结果收集
    sub_results: Annotated[dict[str, str], operator.or_]      # {task_id: result}（并行写入用 or_ reducer）

    # 审核状态
    plan_approved: bool                                       # 方案是否通过审核
    result_approved: bool                                     # 结果是否通过审核
    review_retries: int                                       # 审核重试次数
    max_review_retries: int                                   # 最大审核重试次数
```

### 3.2 与 AgentState 的关系

```
AgentState (已有)
├── messages: list[BaseMessage]          # LangChain 原生消息（LLM/ToolNode 直接使用）
├── ga_messages: list[MessageBlock]      # 自定义消息（Agent 间通信使用）
├── cur_iteration: int                   # 当前迭代
└── max_iteration: int                   # 最大迭代

OrchestrationState (新增, 继承 AgentState)
├── [继承所有 AgentState 字段]
├── phase: OrchestrationPhase            # 编排流程阶段
├── intent: str                          # 用户意图
├── guard_feedback: str                  # 审核反馈
├── task_plan: TaskPlan | None           # 任务计划
├── sub_results: dict[str, str]          # 子任务结果集
├── plan_approved: bool                  # 方案通过标记
├── result_approved: bool                # 结果通过标记
├── review_retries: int                  # 重试次数
└── max_review_retries: int              # 最大重试次数
```

---

## 4. Agent 设计

### 4.1 GuardAgent

**职责**：意图识别 → 方案审核 → 结果验收，全程把控质量

**系统提示词核心内容**：

```
你是一个 GuardAgent（质量把关者），负责：
1. 意图识别：准确理解用户需求，提炼核心意图
2. 方案审核：评估 PlanAgent 提交的执行方案是否合理、可行、完整
3. 结果验收：检查最终结果是否符合用户期望，是否完整准确

审核标准：
- 方案是否覆盖了用户的所有需求点
- 子任务分解是否合理，粒度是否适当
- 子任务之间的依赖关系是否正确
- 输出格式是否符合要求
- 如有问题，必须明确指出需要修改的地方
```

**节点实现**：

```python
# src/graph_agent/orchestration/guard.py

def guard_node(state: OrchestrationState) -> dict:
    """GuardAgent 节点：根据当前 phase 执行对应职责"""
    phase = state["phase"]

    if phase == OrchestrationPhase.INTENT_ANALYSIS:
        return _analyze_intent(state)
    elif phase == OrchestrationPhase.PLAN_REVIEW:
        return _review_plan(state)
    elif phase == OrchestrationPhase.RESULT_REVIEW:
        return _review_result(state)
    else:
        return {}
```

**路由逻辑**：

```python
def guard_router(state: OrchestrationState) -> str:
    """GuardAgent 的路由决策"""
    phase = state["phase"]

    if phase == OrchestrationPhase.INTENT_ANALYSIS:
        return "plan"              # 意图分析完成 → PlanAgent 制定方案

    if phase == OrchestrationPhase.PLAN_REVIEW:
        if state["plan_approved"] or state["review_retries"] >= state["max_review_retries"]:
            return "plan"          # 方案通过或超过重试 → PlanAgent 执行
        return "plan"              # 方案驳回 → PlanAgent 修订

    if phase == OrchestrationPhase.RESULT_REVIEW:
        if state["result_approved"] or state["review_retries"] >= state["max_review_retries"]:
            return END             # 结果通过 → 返回用户
        return "plan"              # 结果驳回 → PlanAgent 修订

    return END
```

### 4.2 PlanAgent

**职责**：任务分解 → 方案制定 → 派发执行 → 结果汇总

**系统提示词核心内容**：

```
你是一个 PlanAgent（任务规划者），负责：
1. 分析 GuardAgent 传递的用户意图
2. 将复杂任务分解为可执行的子任务
3. 为每个子任务匹配最合适的技能（Skill）
4. 制定执行方案（串行/并行顺序、依赖关系）
5. 汇总各 SubAgent 的执行结果，生成最终答案

输出格式要求：
- 每个子任务必须包含：task_id、描述、所需技能、输入数据、依赖关系
- 必须标明执行策略（并行或串行）
- 必须说明期望的输出格式
```

**节点实现**：

```python
# src/graph_agent/orchestration/plan.py

def plan_node(state: OrchestrationState) -> dict:
    """PlanAgent 节点：根据当前 phase 执行对应职责"""
    phase = state["phase"]

    if phase == OrchestrationPhase.PLAN_GENERATION:
        return _generate_plan(state)
    elif phase == OrchestrationPhase.TASK_EXECUTION:
        return _dispatch_tasks(state)
    elif phase == OrchestrationPhase.RESULT_SYNTHESIS:
        return _synthesize_results(state)
    else:
        return {}
```

**任务派发逻辑**：

```python
def _dispatch_tasks(state: OrchestrationState) -> dict:
    """根据执行策略派发子任务"""
    task_plan = state["task_plan"]
    sub_tasks = task_plan.sub_tasks

    # 检查是否可以并行执行
    is_parallel = task_plan.execution_strategy == "parallel"

    dispatch_messages = []
    for task in sub_tasks:
        if task.status == "pending":
            # 检查依赖是否满足
            deps_met = all(
                state["sub_results"].get(dep_id) is not None
                for dep_id in task.dependencies
            )
            if deps_met:
                task.status = "running"
                dispatch_messages.append(
                    _create_task_dispatch_message(task)
                )

    return {
        "task_plan": task_plan,            # 更新任务状态
        "ga_messages": dispatch_messages,  # 派发消息
    }
```

### 4.3 SubAgent

**职责**：执行被分配的子任务，调用工具或自身技能完成工作

**设计要点**：

- 每个 SubAgent 注册时声明自己的 **技能标签（skill tags）**
- PlanAgent 根据 `required_skill` 字段匹配合适的 SubAgent
- SubAgent 可以调用 `ToolCenter` 中的任意工具
- SubAgent 在自己的 ReAct 循环中独立完成子任务

**SubAgent 注册中心**：

```python
# src/graph_agent/orchestration/subagent.py

from dataclasses import dataclass, field


@dataclass
class SubAgentConfig:
    """SubAgent 配置"""
    name: str                        # SubAgent 名称
    description: str                 # 能力描述
    skills: list[str]                # 技能标签列表
    tools: list[str]                 # 可使用的工具名称列表
    system_prompt: str               # 系统提示词
    llm_config: dict | None = None   # 可选独立 LLM 配置
    max_iterations: int = 5          # ReAct 最大迭代次数


class SubAgentRegistry:
    """SubAgent 注册中心"""

    def __init__(self):
        self._agents: dict[str, SubAgentConfig] = {}

    def register(self, config: SubAgentConfig) -> None:
        self._agents[config.name] = config

    def get(self, name: str) -> SubAgentConfig:
        return self._agents[name]

    def find_by_skill(self, skill: str) -> list[SubAgentConfig]:
        """根据技能标签查找匹配的 SubAgent"""
        return [
            agent for agent in self._agents.values()
            if skill in agent.skills
        ]

    def list_all(self) -> list[SubAgentConfig]:
        return list(self._agents.values())
```

**SubAgent 执行流程**（ReAct 循环）：

```
1. 接收任务消息 (MessageBlock, message_type="agent_plan")
2. 解析任务描述和输入数据
3. ReAct 循环:
   a. LLM 分析当前状态，决定下一步行动
   b. 如需调用工具 → 执行工具 → 获得结果
   c. 判断任务是否完成
   d. 未完成且未超迭代 → 回到 a
4. 返回结果 MessageBlock (message_type="agent_response")
```

**SubAgent 路由节点**：

```python
def subagent_router(state: OrchestrationState) -> list[dict]:
    """
    根据 task_plan 中的 sub_tasks，为每个 running 状态的任务
    创建对应的 SubAgent 执行子图调用
    """
    task_plan = state["task_plan"]
    commands = []

    for task in task_plan.sub_tasks:
        if task.status == "running" and not task.result:
            # 查找匹配的 SubAgent
            candidates = registry.find_by_skill(task.required_skill)
            if candidates:
                task.assigned_agent = candidates[0].name
                commands.append({
                    "command": f"go_{candidates[0].name}",
                    "update": {
                        "sub_tasks": task_plan.sub_tasks,
                    }
                })

    return commands
```

---

## 5. 编排图设计

### 5.1 图结构

```
graph LR
    START --> guard_intent[GuardAgent<br/>意图识别]
    guard_intent --> plan_generate[PlanAgent<br/>制定方案]
    plan_generate --> guard_review[GuardAgent<br/>方案审核]
    guard_review -->|驳回| plan_generate
    guard_review -->|通过| plan_dispatch[PlanAgent<br/>派发任务]
    plan_dispatch --> subagent_exec[SubAgent<br/>并行执行]
    subagent_exec --> plan_synthesize[PlanAgent<br/>汇总结果]
    plan_synthesize --> guard_result[GuardAgent<br/>结果审核]
    guard_result -->|驳回| plan_synthesize
    guard_result -->|通过| END
```

### 5.2 节点定义

| 节点名 | 函数 | 触发条件 | 职责 |
|--------|------|---------|------|
| `guard` | `guard_node` | 始终（多阶段复用） | 根据 `phase` 执行意图分析/方案审核/结果审核 |
| `plan` | `plan_node` | Guard 或 SubAgent 完成后 | 根据 `phase` 执行方案制定/任务派发/结果汇总 |
| `sub_exec` | `subagent_exec_node` | PlanAgent 派发任务后 | 并行调用各 SubAgent 执行子任务 |

### 5.3 构建代码

```python
# src/graph_agent/orchestration/graph.py

from langgraph.graph import StateGraph, START, END
from graph_agent.orchestration.state import OrchestrationState, OrchestrationPhase
from graph_agent.orchestration.guard import guard_node, guard_router
from graph_agent.orchestration.plan import plan_node, plan_router
from graph_agent.orchestration.subagent import subagent_exec_node


def build_orchestration_graph() -> StateGraph:
    builder = StateGraph(OrchestrationState)

    builder.add_node("guard", guard_node)
    builder.add_node("plan", plan_node)
    builder.add_node("sub_exec", subagent_exec_node)

    builder.add_edge(START, "guard")

    # Guard → Plan / END
    builder.add_conditional_edges("guard", guard_router, {
        "plan": "plan",
        END: END,
    })

    # Plan → Guard / SubAgents
    builder.add_conditional_edges("plan", plan_router, {
        "guard": "guard",
        "sub_exec": "sub_exec",
        END: END,
    })

    # SubAgents → Plan (结果汇总)
    builder.add_edge("sub_exec", "plan")

    return builder.compile()
```

---

## 6. Skill 系统设计

### 6.1 设计思路

Skill 是对 SubAgent 能力的抽象描述。一个 SubAgent 可以拥有多个 Skill，PlanAgent 在分配任务时根据 `required_skill` 进行匹配。

Skill 与 Tool 的关系：
- **Tool**：原子级能力，如 `read_file`、`calculator`
- **Skill**：组合级能力，如 `data_analysis`（可能包含 read_file + calculator + 等等）
- 一个 Skill 可以映射到多个 Tool，也可以包含 SubAgent 的内部推理能力

### 6.2 Skill 定义

```python
# src/graph_agent/orchestration/skill.py

from dataclasses import dataclass


@dataclass
class Skill:
    """技能定义"""
    name: str                        # 技能名称（唯一标识）
    description: str                 # 技能描述（供 PlanAgent 理解）
    required_tools: list[str]        # 所需的工具列表
    examples: list[str]              # 使用示例
    input_schema: dict               # 输入格式描述
    output_schema: dict              # 输出格式描述
```

### 6.3 预置 Skill 清单

| Skill 名称 | 描述 | 所需工具 | 适用 SubAgent |
|-----------|------|---------|--------------|
| `file_reading` | 读取并解析文件内容 | `read_file` | FileAgent |
| `calculation` | 数学计算与数据分析 | `safe_calculator` | CalcAgent |
| `time_query` | 获取当前时间信息 | `get_utc_time` | TimeAgent |
| `text_summary` | 文本摘要与关键信息提取 | （LLM 原生能力） | SummaryAgent |
| `code_analysis` | 代码分析与理解 | `read_file` + LLM 推理 | CodeAgent |
| `web_search` | 网络信息检索 | （需新增工具） | SearchAgent |

---

## 7. SubAgent 预置清单

| SubAgent 名称 | 绑定 Skill | 系统提示词定位 |  默认 LLM |
|--------------|-----------|--------------|----------|
| `file_agent` | `file_reading` | 文件读取与内容解析专家 | 继承全局 |
| `calc_agent` | `calculation` | 数学计算与数据分析专家 | 继承全局 |
| `time_agent` | `time_query` | 时间信息查询专家 | 继承全局 |
| `summary_agent` | `text_summary` | 文本摘要与信息提炼专家 | 继承全局 |
| `general_agent` | 全部可用 Skill | 通用任务执行者 | 继承全局 |

---

## 8. 消息通信协议

### 8.1 Agent 间消息格式

所有 Agent 间通信均使用 `MessageBlock`，通过 `message_type` 区分消息语义：

| 发送方 → 接收方 | message_type | 携带内容 |
|----------------|-------------|---------|
| GuardAgent → PlanAgent | `guard_intent` | 用户意图描述 + 原始用户消息 |
| PlanAgent → GuardAgent | `plan_proposal` | TaskPlan 结构（含子任务列表） |
| GuardAgent → PlanAgent | `plan_approval` | 审核结果（approved/rejected）+ 反馈 |
| GuardAgent → PlanAgent | `plan_rejection` | 驳回原因 + 修改建议 |
| PlanAgent → SubAgent | `task_assignment` | 子任务描述 + 输入数据 |
| SubAgent → PlanAgent | `task_result` | 子任务执行结果 |
| PlanAgent → GuardAgent | `final_result` | 汇总后的最终结果 |
| GuardAgent → 用户 | `agent_response` | 最终回复内容 |

### 8.2 MessageBlock 示例

**GuardAgent 向 PlanAgent 发送意图**：

```python
MessageBlock(
    role="assistant",
    name="GuardAgent",
    content=[
        ContentBlock(block_type="text", text="用户想读取 D:/test.txt 并计算文件中的数值总和"),
    ],
    message_type="guard_intent",
    message_id="msg_001",
    metadata={
        "original_user_input": "读取 D:/test.txt 并计算总和",
        "intent_type": "file_read_and_calculate",
    }
)
```

**PlanAgent 向 SubAgent 派发任务**：

```python
MessageBlock(
    role="assistant",
    name="PlanAgent",
    content=[
        ContentBlock(block_type="text", text="读取 D:/test.txt 文件内容"),
    ],
    message_type="task_assignment",
    message_id="msg_002",
    metadata={
        "task_id": "task_001",
        "required_skill": "file_reading",
        "assigned_agent": "file_agent",
    }
)
```

---

## 9. 与现有系统的集成

### 9.1 复用关系

| 组件 | 来源 | 使用方式 |
|------|------|---------|
| `MessageBlock` / `ContentBlock` | `message/base.py` | 直接使用，Agent 间通信 |
| `AgentState` | `state.py` | `OrchestrationState` 继承 |
| `ToolCenter` | `tools/base.py` | SubAgent 共享同一 ToolCenter 实例 |
| `LLMFactory` | `llm/base.py` | Guard/Plan 可独立配置不同 LLM |
| `_ensure_synced` | `graph.py` | 编排节点复用同步逻辑 |
| `agent_messages_to_langchain` | `message/convert.py` | SubAgent ReAct 循环内调用 LLM 时使用 |
| `langchain_to_agent_message` | `message/convert.py` | LLM 响应转回 MessageBlock |

### 9.2 与原有 graph.py 的关系

- 原有 `graph.py`（单 Agent ReAct 循环）保持不变，继续作为简单场景入口
- 新 `orchestration/graph.py` 提供三层编排能力，用于复杂任务
- 用户可根据任务复杂度选择使用哪个图

---

## 10. 实现阶段

### 第一阶段：核心框架（1-2 天）
1. 创建 `orchestration/state.py` — `OrchestrationState` 及辅助数据结构
2. 创建 `orchestration/prompts/` — GuardAgent/PlanAgent 系统提示词
3. 创建 `orchestration/guard.py` — GuardAgent 节点（意图分析 + 方案审核 + 结果审核）
4. 创建 `orchestration/plan.py` — PlanAgent 节点（方案制定 + 任务派发 + 结果汇总）
5. 创建 `orchestration/graph.py` — 编排图组装与路由

### 第二阶段：SubAgent 系统（1-2 天）
6. 创建 `orchestration/subagent.py` — SubAgent 注册中心 + ReAct 执行循环
7. 创建 `orchestration/skill.py` — Skill 定义与匹配逻辑
8. 注册预置 SubAgent（file_agent、calc_agent 等）

### 第三阶段：集成与测试（1 天）
9. 更新 `langgraph.json` 注册编排图
10. 编写 `debug_orchestration.py` 交互式调试脚本
11. 编写单元测试和集成测试

---

## 11. 关键设计决策

### 11.1 为什么 GuardAgent 审核用 LLM 而非代码规则？

LLM 审核能理解语义上的质量标准（答案是否完整、逻辑是否合理），代码规则只能做形式检查。初期全部用 LLM 审核，后续可在 GuardAgent 中混合代码规则（如格式校验）以降低 token 消耗。

### 11.2 为什么 SubAgent 可以有自己的 LLM 配置？

不同 SubAgent 的任务复杂度差异大。简单的 `time_agent` 可用轻量模型（如 gpt-3.5-turbo），复杂的 `code_agent` 可用强模型（如 claude-opus），以优化成本。

### 11.3 审核超时的兜底策略？

`max_review_retries`（默认 3）防止 GuardAgent 和 PlanAgent 陷入无限审核循环。超过重试次数后，GuardAgent 接受当前方案/结果并继续。

### 11.4 子任务中的依赖处理？

`SubTask.dependencies` 定义了任务间的依赖关系。PlanAgent 在派发时检查依赖状态，仅当依赖任务完成（`sub_results` 中有结果）后才派发后续任务。无依赖的任务可并行执行。

---

## 12. 扩展方向

- [ ] 支持 GuardAgent 的流式审核（实时反馈修改建议）
- [ ] SubAgent 的 Skill 自动发现机制（类似 ToolCenter.auto_discover）
- [ ] 子任务执行历史持久化与断点续传
- [ ] 支持 GuardAgent 的紧急中断能力（用户打断执行）
- [ ] 支持多层嵌套编排（PlanAgent 可以进一步委托其他 PlanAgent）
- [ ] SubAgent 的负载均衡与池化管理
- [ ] 编排流程的可视化追踪（基于 message_id 的调用链）
- [ ] GuardAgent 的规则引擎混合模式（规则 + LLM 联合审核）
