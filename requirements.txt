# ============================================================
# GraphAgent 运行所需第三方依赖（pip 用户入口）
#
# 版本来源：
#   - `==` 版本取自 uv.lock 的已解析版本（可直接复现）
#   - `>=` 版本为 uv.lock 未覆盖的新增依赖，按兼容性下限
# ============================================================

# ---- LangChain / LangGraph 核心 ----
langchain==1.2.10
langchain-core==1.2.28
langgraph==1.0.10

# ---- LLM Provider ----
langchain-anthropic==1.3.4
langchain-openai>=1.0.0
langchain-ollama>=0.3.0

# ---- 配置 / 环境 ----
python-dotenv==1.2.2
pyyaml==6.0.3

# ---- MCP 协议 ----
mcp>=1.0.0

# ---- ACP HTTP+SSE 服务 ----
fastapi>=0.110.0
sse-starlette==2.1.3
uvicorn==0.41.0

# ---- 网页工具 / ACP 客户端 ----
beautifulsoup4>=4.12.0
requests==2.33.0
aiohttp>=3.10.0

# ---- 文档生成 ----
python-pptx>=0.6.21

# ============================================================
# 开发 / 测试依赖（运行测试与 lint 时才需要，按需取消注释）
# ============================================================
# pytest==9.0.2
# anyio==4.12.1
# ruff==0.15.5
# langgraph-cli[inmem]==0.4.14
